{% extends "baseTemplate.html.twig" %}


{% block content %}
	<div class=" container">
		<div class="card">
			<div class="card-body">

				<div class="container">
					<h3 class="perso-titre">
						Formulaire modification Autres Catégorie
					</h3>

					<a href="{{path("tki_all_categorie_index")}}" class="btn btn-sm  text-white my-3 fw-bold" style="background-color: #FBBB01;">Liste</a>

					{{ form_start(form) }}
					{{ form_errors(form)}}

					<div class="row">
						<div class="col-12 col-md-6">
							{{ form_row(form.description) }}
						</div>
					</div>


					<button type="submit" class="btn btn-primary ajout-overlay">
						<i class="fas fa-save"></i>
						Enregistrer
					</button>
					{{ form_end(form) }}
				</div>

			</div>
		</div>
	{% endblock %}
